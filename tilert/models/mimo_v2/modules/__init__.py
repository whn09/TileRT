"""MiMo-V2.5-Pro runtime modules (decode layer / attention / MoE).

Skeleton only: the real implementations call ``torch.ops.tilert.*`` kernels
provided by ``libtilert_mimo.so``, which is not yet open-sourced. See
:class:`tilert.models.mimo_v2.modules.end2end.ShowHandsMiMoLayer`.
"""
